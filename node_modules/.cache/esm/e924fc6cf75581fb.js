let arg,inquirer,PROJECT_TYPES,createProject;_f75‍.x([["cli",()=>cli]]);_f75‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_f75‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_f75‍.w("./constants",[["PROJECT_TYPES",["PROJECT_TYPES"],function(v){PROJECT_TYPES=v}]]);_f75‍.w("./main",[["createProject",["createProject"],function(v){createProject=v}]]);



function parseArgumentsIntoOptions(rawArgs) {
  const args = arg(
    {
      // "--yes": Boolean,
      // "-y": "--yes",
    },
    {
      argv: rawArgs.slice(2),
    }
  );
  return {
    name: args._[0],
  };
}

async function promptForMissingOptions(options) {
  const questions = [];
  if (!options.template) {
    questions.push({
      type: "list",
      name: "template",
      message: "Please choose which project template to use",
      choices: PROJECT_TYPES.map((i) => i.name),
    });
  }
  if (!options.name) {
    questions.push({
      type: "input",
      name: "name",
      message: "Please input project name",
      default: "AwesomeProject",
    });
  }

  const answers = await inquirer.prompt(questions);
  return {
    ...options,
    template: PROJECT_TYPES.find((i) => i.name === answers.template).template,
    name: options.name || answers.name,
  };
}

       async function cli(args) {
  let options = parseArgumentsIntoOptions(args);
  options = await promptForMissingOptions(options);
  await createProject(options);
}
